## Packet Result
- Packet: 09 — Popover Slim-Down (Stage A complete; Stage B deliberately withheld)
- Branch: speech-engine-exploration-benchmarks
- Commit hash: 8978dfe7 (Stage A only — Stage B not committed, see below)
- Pushed: Yes (Stage A)
- Files changed: `Sources/DexDictate/PopoverHeroView.swift` (new — state dot, status label, trigger hint, Start/Stop button, idle-only watermark), `Sources/DexDictate/PopoverResultView.swift` (new — transcript, Pasted/Copied badge, Retry/Learn Correction), `Sources/DexDictate/PopoverHistoryTeaser.swift` (new — last 3 rows + Open History button), `Sources/DexDictate/PopoverRootView.swift` (new — header/ticker/hero-or-error-banner/result/history/status-line/footer assembly), `Sources/DexDictate/DexDictateApp.swift` (added third branch: `useSlimPopover` → `PopoverRootView`, default false, classic `AntiGravityMainView` unchanged as ultimate fallback), `Sources/DexDictateKit/Settings/AppSettings.swift` (added `useSlimPopover` — a new internal flag, not a change to any existing key)
- Files inspected but not changed: `Sources/DexDictate/ControlsView.swift`, `Sources/DexDictate/HistoryView.swift`, `Sources/DexDictate/PermissionBannerView.swift`, `Sources/DexDictate/PopoverSizing.swift`, `Sources/DexDictate/SurfaceTokens.swift` (all read in full per required inspection), `Sources/DexDictateKit/EngineLifecycle.swift` (confirmed `EngineState` has no `.paused` case), `Sources/DexDictateKit/TranscriptionFeedback.swift` (confirmed `.pastedToActiveApp`/`.copiedOnlySensitiveContext` cases back the badge), `Sources/DexDictateKit/TranscriptionHistory.swift` (confirmed `items` is most-recent-first via `add()`, so `.prefix(3)` is correct)
- Forbidden files touched: No (`FlavorTickerView.swift` and `WatermarkAssetProvider.swift` — zero-line diffs, confirmed by `git diff --stat`)
- Tests run: full `swift test`; targeted `swift test --filter OutputPipelineHardening`
- Test result: 382 passed, 0 failures (full suite, matches Packet 01 baseline); 17/17 passed (targeted)
- Targeted tests: `OutputPipelineHardeningTests` — 17/17 passed
- Manual validations: not performed — see `NEEDS_ANDREW.md` for the full list and why the profile-switch check specifically can't be meaningfully done yet
- Screenshots captured: none — blocked, same automation gap as every prior packet
- Feature-loss checklist rows completed: not exercised (Stage B, which would make the new popover the one users interact with, was withheld)
- Dexter preservation checks: not exercised — see Known issues
- Known issues: **Stage B (default flip) deliberately withheld.** Full reasoning in `NEEDS_ANDREW.md`. Summary: the new popover contract has no UI for Profile switching, Theme selection, or ticker on/off — those are still exclusively in the classic popover's Quick Settings stack, assigned to Packet 11 (not yet run), and there is no fallback UI path back to the classic popover once `useSlimPopover`'s default flips (unlike `useExperimentalStateFirstUI`, which has a header toggle button). Flipping now would make profile/theme switching unreachable in the real running app until Packet 11 completes. **Wireframe deviation**: "Pause Dictation" in the footer overflow calls the same `engine.stopSystem()` as Stop — `EngineState` has no distinct paused state, and adding one would require editing the forbidden `TranscriptionEngine.swift`; documented as a same-behavior relabel, not a new feature. **Wireframe addition**: added `.keyboardShortcut(",", modifiers: [.command])` to the footer's Settings button, giving ⌘, real behavior scoped to the popover's own view hierarchy — resolves the gap Packet 02 flagged (no app menu to hang a global Settings scene off of) without touching `MenuBarExtra`/app-menu structure.
- Rollback required: No (Stage A is safe and additive; nothing to roll back)
- Next recommended packet: 10 — Terminology Sweep, then 11 — Dexter & Personality, then return to complete Packet 09 Stage B once Packet 11's Quick Settings inventory is clean
