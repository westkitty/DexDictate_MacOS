## Packet Result
- Packet: 09 — Popover Slim-Down (Stages A+B) — Stage B completed after Packet 11
- Branch: speech-engine-exploration-benchmarks
- Commit hash: 8978dfe7 (Stage A), (Stage B — see AUTOPILOT_RUN_LEDGER.md)
- Pushed: Yes (both)
- Files changed: **Stage A** — `Sources/DexDictate/PopoverHeroView.swift` (new — state dot, status label, trigger hint, Start/Stop button, idle-only watermark), `Sources/DexDictate/PopoverResultView.swift` (new — transcript, Pasted/Copied badge, Retry/Learn Correction), `Sources/DexDictate/PopoverHistoryTeaser.swift` (new — last 3 rows + Open History button), `Sources/DexDictate/PopoverRootView.swift` (new — header/ticker/hero-or-error-banner/result/history/status-line/footer assembly), `Sources/DexDictate/DexDictateApp.swift` (added third branch: `useSlimPopover` → `PopoverRootView`), `Sources/DexDictateKit/Settings/AppSettings.swift` (added `useSlimPopover`, a new internal flag). **Stage B** — `Sources/DexDictateKit/Settings/AppSettings.swift` (single-line default flip: `useSlimPopover: Bool = false` → `true`; comment updated). Between the two stages, Packet 11 also touched `PopoverRootView.swift` (added theme background handling + header seam fix) and `Sources/DexDictate/ChromeButton.swift` (optional tint parameter) — already reported under Packet 11.
- Files inspected but not changed: `Sources/DexDictate/ControlsView.swift`, `Sources/DexDictate/HistoryView.swift`, `Sources/DexDictate/PermissionBannerView.swift`, `Sources/DexDictate/PopoverSizing.swift`, `Sources/DexDictate/SurfaceTokens.swift` (all read in full per required inspection), `Sources/DexDictateKit/EngineLifecycle.swift` (confirmed `EngineState` has no `.paused` case), `Sources/DexDictateKit/TranscriptionFeedback.swift` (confirmed `.pastedToActiveApp`/`.copiedOnlySensitiveContext` cases back the badge), `Sources/DexDictateKit/TranscriptionHistory.swift` (confirmed `items` is most-recent-first via `add()`, so `.prefix(3)` is correct)
- Forbidden files touched: No (`FlavorTickerView.swift` and `WatermarkAssetProvider.swift` — zero-line diffs across both stages)
- Tests run: full `swift test` (both stages); targeted `swift test --filter OutputPipelineHardening` (Stage A)
- Test result: Stage A — 382 passed, 0 failures; 17/17 passed (targeted). Stage B — first run hit the documented known-flaky `MainActorActionTests.testRunAsyncExecutesOnMainActor` alone, reran once per the allowance, second run: 382 passed, 0 failures.
- Targeted tests: `OutputPipelineHardeningTests` — 17/17 passed (Stage A)
- Manual validations: not performed — see `NEEDS_ANDREW.md`
- Screenshots captured: none — blocked, same automation gap as every prior packet
- Feature-loss checklist rows completed: not manually exercised (blocked by the same automation gap); code-level verification only — same engine APIs called as the classic popover, same published state subscribed to
- Dexter preservation checks: `FlavorTickerView.swift`/`WatermarkAssetProvider.swift` zero-line diffs confirmed; ticker/watermark render via the same untouched provider APIs
- Known issues: **Stage B was withheld at first commit**, then completed as a follow-up once Packet 11 gave Profile/Theme/Ticker a Settings-window home — full reasoning in `NEEDS_ANDREW.md`. Fresh inventory before flipping confirmed every `showLegacy*` flag in `QuickSettingsView.swift` is `false`; the 4 orphaned benchmark controls flagged in Packet 10 were already hidden since Packet 07 and unaffected by this flip. **Wireframe deviation**: "Pause Dictation" in the footer overflow calls the same `engine.stopSystem()` as Stop — `EngineState` has no distinct paused state, and adding one would require editing the forbidden `TranscriptionEngine.swift`; documented as a same-behavior relabel, not a new feature. **Wireframe addition**: added `.keyboardShortcut(",", modifiers: [.command])` to the footer's Settings button, giving ⌘, real behavior scoped to the popover's own view hierarchy — resolves the gap Packet 02 flagged (no app menu to hang a global Settings scene off of) without touching `MenuBarExtra`/app-menu structure.
- Rollback required: No (Stage B is a single-line, independently revertible commit per the packet's own rollback plan)
- Next recommended packet: 12A — Experimental Adoption Inventory
