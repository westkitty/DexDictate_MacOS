## Packet Result
- Packet: 14 — Live Preview Prototype (L1)
- Branch: speech-engine-exploration-benchmarks
- Commit hash: (see AUTOPILOT_RUN_LEDGER.md)
- Pushed: Yes
- Files changed:
  - New: `Sources/DexDictateKit/LivePreview/LivePreviewController.swift` — subscribes to already-`@Published` `TranscriptionEngine.state`/`.liveTranscript`/`.inputLevel` (no new audio tap, no new streaming session — reuses whatever the engine already resolved via `TranscriptionProviderRegistry`); throttles caption updates to ≤4/sec (`.throttle(for: .milliseconds(250)...)`); a health-poll-based kill switch (see "Known issues" for why this, not a literal provider-error hook)
  - New: `Sources/DexDictate/LivePreview/LivePreviewCaptionView.swift` — dimmed/italic caption + "PREVIEW" capsule tag, "Finalizing…" handoff badge, whole-line replacement only
  - New: `Tests/DexDictateTests/LivePreviewInvariantTests.swift` — the required byte-identical invariant test + a disabled-by-default inertness test
  - Modified: `Sources/DexDictateKit/Settings/AppSettings.swift` (new key `livePreviewEnabled`, default `false`); `Sources/DexDictate/SettingsWindow/DictationSettingsPage.swift` (new toggle "Live Preview (experimental)"); `Sources/DexDictate/PopoverHeroView.swift` + `PopoverRootView.swift` (thread `LivePreviewController`, render `LivePreviewCaptionView` below the Start/Stop button); `Sources/DexDictate/FloatingHUD.swift` (thread `LivePreviewController` into the standard HUD, render the caption; extracted `statusContent` as its own `@ViewBuilder` — required to fix a SwiftUI type-checker timeout, not a design change); `Sources/DexDictate/DexDictateApp.swift` (new `@StateObject`, idempotent `.start(engine:)` in the existing `.onAppear`, threaded into `hudController.setup(...)`)
- Files inspected but not changed: `Sources/DexDictateKit/TranscriptionEngine.swift` (forbidden — read to confirm `liveTranscript`/`inputLevel` are genuinely safe, already-published observation points, and that `provider.onPartialResult`/`.onError` are single-slot closures the engine already owns — confirming a NEW consumer must never set them too); `Sources/DexDictateKit/Transcription/TranscriptionProviderRegistry.swift`, `NemotronTranscriptionProvider.swift`, `AppleSpeechTranscriptionProvider.swift` (confirmed `healthCheck()`/`refreshHealthReport()` are cheap, synchronous, side-effect-free — safe to poll periodically); `Sources/VerificationRunner/main.swift` (confirmed the existing `runBenchmark`/`runBenchmarkCorpus` load→resample→transcribe technique, reused unchanged in the invariant test); `Package.swift` (confirmed the test target depends only on `DexDictateKit`/`DexDictateObjCSupport`, not the `DexDictate` executable target — this is why `LivePreviewController` lives in `DexDictateKit` rather than the packet's originally-suggested `Sources/DexDictate/LivePreview/` path; see "Known issues")
- Forbidden files touched: No
- Tests run: full `swift test`; targeted `swift test --filter MainActorDispatchTests` (rerun of an unrelated flaky failure)
- Test result: 409 passed, 0 failures on final run. First run showed 1 failure (`MainActorDispatchTests.testAsyncRunsOnMainThreadAsynchronously`, an async-ordering race — same failure family as the documented `MainActorActionTests` known-flaky allowance, but a different test name/file, unrelated to Live Preview or Whisper). Rerun via `--filter MainActorDispatchTests`: passed clean. Per the known-flaky allowance's spirit, documented and continuing.
- Targeted tests: **`LivePreviewInvariantTests` — 2/2 passed**, including the required invariant: `testCommittedTranscriptIsByteIdenticalWithPreviewControllerAttachedOrNot` (2.367s, real tiny.en model, real `sample_corpus/sample.wav`) proves a real, subscribed, actively-observing `LivePreviewController` attached to a real `TranscriptionEngine` produces a byte-identical `WhisperService` transcript versus no controller existing at all. `testPreviewStaysInertWhenSettingIsOff` confirms zero caption/mic-level activity when the toggle is off.
- Manual validations: **Latency comparison**: ran `./scripts/benchmark.sh --audio sample_corpus/sample.wav --model tiny.en --build debug --iterations 3` — median 392ms, mean 455.33ms, min 386ms, max 588ms. This benchmark path does not construct or reference `LivePreviewController` at all (confirmed via `grep` — `VerificationRunner/main.swift` has zero references to it), so "unchanged" here isn't a before/after diff, it's a structural guarantee: the code paths are disjoint, so there is nothing to regress. **Failure simulation**: NOT performed as literally specified ("kill/throw in a controlled way through the provider's own error path"). This packet's kill switch deliberately does NOT hook `provider.onError` — doing so would silently overwrite `TranscriptionEngine`'s own single-slot closure assignment on the same shared provider instance, corrupting its internal `liveTranscript` relay (a real, verified risk, not a hypothetical one — see the file's doc comment). The kill switch instead polls `TranscriptionProviderRegistry.refreshHealthReport()` every 2s while listening; this logic was verified by code review and by the passing invariant test (which confirms `isKilledForSession` stays `false` on the happy path) but a live "provider actually becomes unavailable mid-utterance" scenario was not exercised end-to-end, since forcing that deterministically would require new test seams in provider classes beyond this packet's scope — see `NEEDS_ANDREW.md`.
- Screenshots captured: none — blocked, same automation gap as every prior packet
- Feature-loss checklist rows completed: **Local Whisper Dictation** — untouched; `TranscriptionEngine.swift`/`WhisperService.swift` zero-diff confirmed via `git diff`; the benchmark run above used the exact same code path as before this packet. **Live Transcription (Requirement)** — batch Whisper remains the sole committed-output path (`LivePreviewController` has no reference to `WhisperService` or `OutputCoordinator` anywhere in its source); the preview channel is architecturally isolated, confirmed by the invariant test using the real production classes, not mocks.
- Dexter preservation checks: n/a — no Dexter-adjacent files touched
- Known issues:
  1. **File location deviation**: `LivePreviewController.swift` lives in `Sources/DexDictateKit/LivePreview/`, not the packet's suggested `Sources/DexDictate/LivePreview/`. Reason: `Package.swift`'s `DexDictateTests` target depends only on `DexDictateKit`/`DexDictateObjCSupport`; adding a dependency on the `DexDictate` executable target (which carries a SwiftUI `@main` entry point) to enable `@testable import DexDictate` for the invariant test was assessed as carrying real, hard-to-predict build risk. Moving the controller into `DexDictateKit` (it only needs Combine/Foundation, not SwiftUI — several existing `ObservableObject` classes already live there) sidesteps this entirely with zero `Package.swift` changes. The SwiftUI caption view (`LivePreviewCaptionView.swift`) stays in `Sources/DexDictate/` as intended.
  2. **Kill-switch proxy, not a literal error hook**: see "Manual validations" above — documented in the controller's own doc comment, not hidden.
  3. **`FloatingHUD.swift`'s `body` required extraction into `statusContent`** to fix a SwiftUI type-checker timeout after adding the caption row — a compiler-budget fix, zero layout/behavior change (same view tree, just split across two properties).
  4. Nano HUD (`DexNanoHUDView.swift`) does NOT get the preview caption — only the standard `FloatingHUDView` and the popover hero do, per the goal's "and/or" wording. Left untouched to avoid risking the already-adopted Nano HUD component.
  5. No new `@AppStorage` keys beyond `livePreviewEnabled` (default `false`).
- Rollback required: No
- Next recommended packet: none (end of pack) or as Andrew directs — Packet 16 (Final Integration QA) remains, per this campaign's own sequence
