## Packet Result
- Packet: 12B — Experimental UI Retirement
- Branch: speech-engine-exploration-benchmarks
- Commit hash: (see AUTOPILOT_RUN_LEDGER.md)
- Pushed: Yes
- Files changed:
  - Deleted: `Sources/DexDictate/ExperimentalUI/DexStateFirstPopoverView.swift`, `DexExperimentalEntry.swift`, `DexExperimentalUIStateAdapter.swift`, `DexLayeredRevealView.swift` (the retired state-first surface itself — confirmed via grep that nothing outside `ExperimentalUI/` referenced any of their types)
  - Moved (git mv, unchanged content except a doc-comment update) out of `ExperimentalUI/` into `Sources/DexDictate/`: `DexCommandPaletteView.swift`, `DexNanoHUDView.swift`, `DexStateFirstComponents.swift` — all three are load-bearing for standard-UI adoptions from Packets 12A/12A-B (`DexContextChips`, `DexOutputChips`, `DexTranscriptCard`, `DexMicMeter`, `DexterCommentaryLine` live in `DexStateFirstComponents.swift`)
  - Moved + trimmed: `DexDexterFeedView.swift` (moved, doc comment updated — no longer flag-gated); `DexExperimentalHubViews.swift` (moved, retired `DexExperimentalGUISwitcherView` deleted from it — the actual "Switch UI" affordance the handoff names for removal — kept only the load-bearing `DexExperimentalFeatureHubView`, which `FloatingHUD.swift`'s `showHubPanel()` depends on for the Nano HUD's "All Features" access point)
  - `Sources/DexDictate/DexDictateApp.swift`: removed the `if settings.useExperimentalStateFirstUI { DexExperimentalEntry(...) }` branch (now just `if useSlimPopover {...} else {...}`); removed the `UIModeToggleButton` struct and its call site in `AntiGravityMainView`'s header (the classic popover's "Switch UI affordance")
  - `Sources/DexDictate/SettingsWindow/AdvancedPage.swift`: removed the "State-first compact popover", "Command palette", and "Dexter stateful feed" toggle rows (their sole readers were in the deleted files — leaving them would present non-functional switches); kept "Nano HUD" (its reader remains real, in production `FloatingHUD.swift`)
  - `Sources/DexDictate/DexCommandPaletteView.swift`: removed its own "Toggle State-first Popover" command (would have silently no-opped post-retirement — a real dangling-reference bug in an already-adopted standard component, fixed as part of this retirement, not scope creep)
  - `Sources/DexDictate/HelpView.swift`: rewrote the "Experimental UI" help topic body + search keywords to describe current reality (Command Palette/Dexter Feed always-on, Nano HUD still opt-in, state-first surface retired) instead of the now-nonexistent surface
  - `Sources/DexDictate/ModelSelectionActions.swift`, `Sources/DexDictate/QuickSettingsView.swift`: one doc-comment each, updated to stop calling the now-standard `DexContextChips` "Experimental"
- Files inspected but not changed: all forbidden files (see below); `Sources/DexDictate/FloatingHUD.swift` (confirmed its `showHubPanel()` dependency on `DexExperimentalFeatureHubView` before deciding to move rather than delete that type); `Sources/DexDictate/QuickSettingsView.swift`'s legacy hidden toggle rows (`showLegacyExperimentalUICard` block, lines ~695-722) — left completely alone per this packet's own stop condition ("the legacy standard popover files, hidden since Packet 09, would be swept up... this packet removes ONLY the ExperimentalUI/ surface and the Switch UI affordance"); `Tests/DexDictateTests/DexExperimentalUITests.swift` (confirmed it only tests `DexDictateKit` display-state structs, none of the moved/deleted view files — unaffected, unchanged)
- Forbidden files touched: No
- Tests run: full `swift test`
- Test result: 382 passed, 0 failures (matches Packet 01 baseline)
- Targeted tests: none specified by this packet beyond the full suite
- Manual validations: **FULL feature-loss checklist result: all 24 Critical Preservation Check rows verified at the code level — every forbidden/fragile file listed in the checklist (`TranscriptionEngine.swift`, `AudioRecorderRecoverySupport.swift`, `AudioTapInstaller.m`, `SecureInputContext.swift`, `ClipboardManager.swift`, `OutputCoordinator.swift`, `OnboardingView.swift`, `WatermarkAssetProvider.swift`, `FlavorTickerView.swift`, `ProfileManager.swift`, `LaunchIntroController.swift`) shows zero diff via `git diff`; every UI exposure point this packet could have affected was individually confirmed still present in source: Command Palette (⌘K, unconditional in `PopoverRootView`), Nano HUD (toggle preserved in `AdvancedPage`, reader intact in `FloatingHUD.swift`), inline Dexter result quote (untouched, Packet 11/12A), Dexter Feed (unconditional in `DexterPersonalityPage`), compact controls + live transcript/mic meter (unconditional in `PopoverRootView`), the Nano HUD's "All Features" hub panel (`DexExperimentalFeatureHubView`, moved not deleted). Only the state-first popover surface and the Switch UI affordance are gone — both explicitly named for retirement, not "capabilities." Live walkthrough steps requiring speech input, clicking through the running app, or visual confirmation (HUD meter movement, ticker scrolling, launch animation playback, onboarding wizard pages) could NOT be performed — same LSUIElement/accessibility automation blocker documented in every packet this entire session (computer-use cannot target this LSUIElement app; shell lacks Accessibility permission for AppleScript). This is a code-level verification, not a substitute for Andrew's own manual click-through.**
- Screenshots captured: none — blocked, same automation gap as every prior packet
- Feature-loss checklist rows completed: ALL (verified at code level as described above; live/manual steps blocked — see above)
- Dexter preservation checks: `ProfileManager.swift`, `WatermarkAssetProvider.swift`, `FlavorTickerView.swift` — zero-line diffs confirmed via `git diff`. Dexter Feed (`DexDexterFeedView.swift`) relocated but logic byte-identical (only its doc comment changed).
- Known issues: `DexExperimentalHubViews.swift` was split rather than moved wholesale — `DexExperimentalFeatureHubView` (load-bearing) kept and relocated; `DexExperimentalGUISwitcherView` (the actual "Switch UI" surface) deleted. This is a judgment call beyond the handoff's literal file list (which named the whole file for removal) because a literal-file-list deletion would have broken `FloatingHUD.swift`'s existing, real, unrelated "hub panel" dependency — documented here rather than silently deviating. Three Advanced-page toggle rows removed (state-first, command palette, Dexter feed flags) since their sole readers no longer exist anywhere in the codebase after this retirement; their underlying `@AppStorage` keys were NOT removed or renamed (still defined in `AppSettings.swift`, untouched) — only the now-nonfunctional Settings UI rows referencing them are gone, per "no storage-key changes."
- Rollback required: No
- Next recommended packet: 15 — Visual Polish (already completed earlier in this campaign) — next in the approved sequence is 13, since Andrew has separately approved attempting it in this same campaign
